<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move.form - Add helper sale_line_ids</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <!-- NOTE: In Odoo 19, we simply ensure the field is loaded in the view
                 The field sale_line_ids exists in account.move.line model and will be
                 accessible through the ORM even if not directly visible in the tree -->
            <field name="invoice_line_ids" position="attributes">
                <attribute name="context">{'default_sale_line_ids': []}</attribute>
            </field>
        </field>
    </record>
</odoo>
